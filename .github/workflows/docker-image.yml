name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Build the broker Docker image
      run: docker build ./wikimedia_messages/kafka_broker --file ./wikimedia_messages/kafka_broker/Dockerfile --tag devops/broker:$(date +%s)

    - name: Build the zookeeper Docker image
      run: docker build ./wikimedia_messages/kafka_zookeeper --file ./wikimedia_messages/kafka_zookeeper/Dockerfile --tag devops/zookeeper:$(date +%s)

    - name: Build the consumer Docker image
      run: docker build ./wikimedia_messages/kafka_consumer --file ./wikimedia_messages/kafka_consumer/Dockerfile --tag devops/consumer:$(date +%s)

    - name: Build the producer Docker image
      run: docker build ./wikimedia_messages/kafka_producer --file ./wikimedia_messages/kafka_producer/Dockerfile --tag devops/producer:$(date +%s)

    - name: Log in to Docker Hub
      run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}

    - name: Push broker Docker image
      run: docker push ${{ secrets.DOCKER_USERNAME }}/devops/broker:$(date +%s)

    - name: Push zookeeper Docker image
      run: docker push ${{ secrets.DOCKER_USERNAME }}/devops/zookeeper:$(date +%s)

    - name: Push consumer Docker image
      run: docker push ${{ secrets.DOCKER_USERNAME }}/devops/consumer:$(date +%s)

    - name: Push producer Docker image
      run: docker push ${{ secrets.DOCKER_USERNAME }}/devops/producer:$(date +%s)

    - name: Deploy using Docker Compose
      run: docker-compose -f ./wikimedia_messages/docker-compose.yaml up
